pre-commit:
  parallel: true
  commands:
    mix-format:
      root: athanor/
      run: mix format --check-formatted
    mix-compile:
      root: athanor/
      run: mix compile --warnings-as-errors

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
